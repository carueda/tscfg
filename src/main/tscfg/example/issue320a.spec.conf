// GenOpts: --skip-gen4tests

# Direct use of ignored objects via __ prefix.
# No use of @define (or any annotations/comments) in this example.

__A = {
  a: string
}

__B = ${__A} {
  b: boolean
}

__AB = ${__A} ${__B}

someAB : ${__AB}
